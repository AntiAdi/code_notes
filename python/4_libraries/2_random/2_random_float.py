import random

#From 0 to 1 (both inclusive)
rand_float = random.random()
print("Random float between 0 and 1 is",rand_float)

#From 1 to 100 (both inclusive)
rand_float_1 = random.uniform(1,100)
print("Random float between 1 and 100 is", rand_float_1)